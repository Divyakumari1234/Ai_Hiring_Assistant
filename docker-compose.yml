services:
  web:
    build: .
    env_file:
      - ./backend/.env
    environment:
      PORT: "10000"
      REVIEW_USERNAME: reviewer
      REVIEW_PASSWORD: ${REVIEW_PASSWORD:?Set REVIEW_PASSWORD before starting the hosted workspace}
    ports:
      - "8080:10000"
    restart: unless-stopped
